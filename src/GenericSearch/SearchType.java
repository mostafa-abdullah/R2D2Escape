package GenericSearch;

public enum SearchType {
    BF,  // Breadth-First
    DF,  // Depth-First
    ID,  // Iterative Deepening
    UC,  // Uniform Cost
    GR1, // Greedy (Heuristic 1)
    GR2, // Greedy (Heuristic 2)
    AS1, // A* (Heuristic 1)
    AS2  // A* (Heuristic 2)
}
